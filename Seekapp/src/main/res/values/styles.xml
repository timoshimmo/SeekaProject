<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white_color</item>
        <item name="android:textColorPrimary">@color/dark_gray</item>
    </style>

    <!-- Base application theme. -->
    <style name="BarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white_color</item>
        <item name="android:textColorPrimary">@color/dark_gray</item>
        <item name="android:textColorSecondary">@color/dark_gray</item>
        <item name="actionMenuTextColor">@color/dark_gray</item>
    </style>

    <style name="StyledIndicators" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="vpiCirclePageIndicatorStyle">@style/CustomCirclePageIndicator</item>
    </style>

    <style name="StyledIndicatorsCourse" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="vpiCirclePageIndicatorStyle">@style/CustomCirclePageIndicatorImage</item>
    </style>

    <style name="CustomCirclePageIndicator">
        <item name="fillColor">@color/pblue_color</item>
        <item name="strokeColor">@color/pblue_color</item>
        <item name="strokeWidth">1dp</item>
        <item name="radius">@dimen/page_indicator_radius</item>
        <item name="centered">true</item>
    </style>

    <style name="CustomCirclePageIndicatorImage">
        <item name="fillColor">@color/white_color</item>
        <item name="strokeColor">@color/white_color</item>
        <item name="strokeWidth">1dp</item>
        <item name="radius">3dp</item>
        <item name="centered">true</item>
    </style>

    <style name="HomeTheme" parent="Theme.AppCompat">
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
    </style>

    <style name="MY.DIALOG" parent="android:Theme" >
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="CustomDialogTheme" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="RatingBar_style" parent="@android:style/Widget.Holo.RatingBar.Small">
        <item name="android:minHeight">13dp</item>
        <item name="android:maxHeight">14dp</item>
        <item name="android:height">12dp</item>
        <item name="android:width">13dp</item>
    </style>

    <style name="RatingBar_reviews_style" parent="@android:style/Widget.DeviceDefault.Light.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/ratingbar_style</item>
        <item name="android:minHeight">@dimen/course_info_ratingbar_height</item>
        <item name="android:maxHeight">@dimen/course_info_ratingbar_height</item>
        <item name="android:height">@dimen/course_info_ratingbar_height</item>
        <item name="android:width">13dp</item>
    </style>

    <style name="RatingBar_unlock_style" parent="@android:style/Widget.DeviceDefault.Light.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/unlock_rating_style</item>
        <item name="android:minHeight">12dp</item>
        <item name="android:maxHeight">12dp</item>

    </style>

    <style name="FullscreenTheme" parent="android:Theme.NoTitleBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">@style/ButtonBar</item>
        <item name="metaButtonBarButtonStyle">@style/ButtonBarButton</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarStyle -->
    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarButtonStyle -->
    <style name="ButtonBarButton" />

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/white_color</item>
        <item name="android:textAppearance">@style/MyCustomTextAppearance</item>
        <item name="tabSelectedTextColor">@color/seeka_blue</item>
    </style>

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">11sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">#646969</item>

    </style>

    <style name="SearchView">
        <item name="floatingSearch_backgroundColor">#ffffff</item>
        <item name="floatingSearch_viewTextColor">@color/dark_gray</item>
        <item name="floatingSearch_hintTextColor">@color/super_light_gray</item>
        <item name="floatingSearch_dividerColor">@color/background_gray</item>
    </style>

</resources>
